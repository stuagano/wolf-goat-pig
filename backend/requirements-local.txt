# Local development requirements for Python 3.13+
# Using flexible versions to avoid wheel build issues

fastapi
uvicorn[standard]
SQLAlchemy
python-multipart
httpx
python-dotenv
pydantic

# Type checking and linting
mypy
ruff

# Pre-commit hooks
pre-commit

# For local development, we use SQLite (no PostgreSQL needed)
# psycopg2-binary is only needed for production PostgreSQL on Render