{
  "name": "wolf-goat-pig-booking-service",
  "version": "1.0.0",
  "description": "Headless browser microservice for ForeTees tee time booking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx playwright install --only-shell chromium || echo 'Playwright install failed, will retry at startup'"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "engines": {
    "node": ">=22"
  }
}
